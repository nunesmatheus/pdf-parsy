version: '3'
services:
  app:
    container_name: pdf-parsy
    build: .
    tty: true
    stdin_open: true
    volumes:
      - .:/app/
      - pip:/usr/local/lib/python3.9/site-packages/

volumes:
  pip:
